app.controller('SeriesShowCtrl', ['$scope', '$http', '$location', '$routeParams', function($scope, $http, $location, $routeParams){

  $http.get( "<%= ENV['URL'] %>" + 'series/' + $routeParams.id).success(function(response){
    $scope.serie = response;
  });
}]);