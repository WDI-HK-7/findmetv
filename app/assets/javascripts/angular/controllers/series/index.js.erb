app.controller('SeriesIndexCtrl', ['$scope', '$http', '$location', function($scope, $http, $location){

  var url = "<%= ENV['URL'] %>" || "http://localhost:3000";

  $http.get(url + '/series').success(function(response){
    console.log(response);
    $scope.series = response;
  });
  
}]);